{#
/**
 * @file
 * Theme override for the map region.
 *
 * This region contains JS-injected content (Leaflet map), so we skip
 * the empty-region check that the base region template performs.
 */
#}
{%
  set classes = [
    'region',
    'region--' ~ region|clean_class,
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div class="region-inner">
      {{ content }}
    </div>
  </div>
{% endif %}
